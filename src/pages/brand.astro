---
import { BRAND } from '../config/brand';
import Layout from '../layouts/Layout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
---

<Layout title={`Brand — ${BRAND.name}`} description={`${BRAND.name} brand identity guidelines: logo, color palette, typography, voice, components, and design tokens.`} robots="noindex,follow">
  <Navbar />

  <!-- Cover -->
  <header class="brand-cover">
    <picture class="brand-cover-bg">
      <source srcset="/hero-bg.webp" type="image/webp">
      <img src="/hero-bg.jpg" alt="" loading="eager">
    </picture>
    <div class="brand-cover-fade"></div>
    <div class="brand-cover-content">
      <div class="brand-cover-logo">
        <svg width="44" height="44" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
          <path d="M21.44 11.05l-9.19 9.19a6 6 0 0 1-8.49-8.49l9.19-9.19a4 4 0 0 1 5.66 5.66l-9.2 9.19a2 2 0 0 1-2.83-2.83l8.49-8.48" />
        </svg>
      </div>
      <h1 class="brand-cover-title">{BRAND.name}</h1>
      <p class="brand-cover-subtitle">Brand Identity</p>
      <p class="brand-cover-version">v1.0 — February 2026</p>
    </div>
  </header>

  <main>

    <!-- Intro -->
    <section class="brand-section" id="intro">
      <div class="container">
        <div class="brand-intro-grid">
          <div>
            <span class="brand-badge">Overview</span>
            <h2 class="heading-serif">The control plane<br>for AI agents.</h2>
          </div>
          <div>
            <p class="brand-intro-text">Paperclip CN is a Chinese-enhanced fork of Paperclip for orchestrating teams of AI agents. Hire AI employees, set goals, automate jobs, and keep your business observable end to end.</p>
            <p class="brand-intro-text brand-muted">This document defines the visual and verbal identity of the Paperclip CN brand. It captures the colors, typography, logo, voice, and component patterns used across penclip.ing, paperclipai.cn, and related surfaces.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Logo -->
    <section class="brand-section" id="logo">
      <div class="container">
        <hr class="brand-rule">
        <span class="brand-badge">Logo</span>
        <h2 class="heading-serif">The Paperclip CN Mark</h2>
        <p class="brand-lede">A minimalist, stroke-drawn monogram built around the Paperclip CN "P". Functional. Recognizable at any size. Always rendered as a stroke, never filled.</p>

        <div class="brand-logo-showcase">
          <div class="brand-logo-card brand-logo-card--light">
            <div class="brand-logo-lockup">
              <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="#1a1a1a" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                <path d="M21.44 11.05l-9.19 9.19a6 6 0 0 1-8.49-8.49l9.19-9.19a4 4 0 0 1 5.66 5.66l-9.2 9.19a2 2 0 0 1-2.83-2.83l8.49-8.48" />
              </svg>
              <span class="brand-logo-wordmark" style="color:#1a1a1a">{BRAND.name}</span>
            </div>
            <span class="brand-logo-label">Primary — Light</span>
          </div>
          <div class="brand-logo-card brand-logo-card--dark">
            <div class="brand-logo-lockup">
              <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="#f0f0f0" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                <path d="M21.44 11.05l-9.19 9.19a6 6 0 0 1-8.49-8.49l9.19-9.19a4 4 0 0 1 5.66 5.66l-9.2 9.19a2 2 0 0 1-2.83-2.83l8.49-8.48" />
              </svg>
              <span class="brand-logo-wordmark" style="color:#f0f0f0">{BRAND.name}</span>
            </div>
            <span class="brand-logo-label" style="color:rgba(255,255,255,0.5)">Primary — Dark</span>
          </div>
          <div class="brand-logo-card brand-logo-card--light">
            <div class="brand-logo-lockup">
              <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="#1a1a1a" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
                <path d="M21.44 11.05l-9.19 9.19a6 6 0 0 1-8.49-8.49l9.19-9.19a4 4 0 0 1 5.66 5.66l-9.2 9.19a2 2 0 0 1-2.83-2.83l8.49-8.48" />
              </svg>
            </div>
            <span class="brand-logo-label">Icon Only</span>
          </div>
          <div class="brand-logo-card brand-logo-card--dark" style="padding:1.5rem;">
            <svg xmlns="http://www.w3.org/2000/svg" width="48" height="48" viewBox="0 0 32 32" fill="none">
              <rect width="32" height="32" rx="6" fill="#1a1a1a"/>
              <path d="M13 5a6 6 0 0 1 6 6v14a4 4 0 0 1-8 0V11a2 2 0 0 1 4 0v12" stroke="#f0f0f0" stroke-width="2.5" stroke-linecap="round" fill="none"/>
            </svg>
            <span class="brand-logo-label" style="color:rgba(255,255,255,0.5)">Favicon</span>
          </div>
        </div>

        <div class="brand-logo-rules">
          <div class="brand-rule-item">
            <span class="brand-rule-icon brand-rule-icon--do">&#10003;</span>
            <p>Always use the stroke-based mark. Never fill the Paperclip CN symbol.</p>
          </div>
          <div class="brand-rule-item">
            <span class="brand-rule-icon brand-rule-icon--do">&#10003;</span>
            <p>Maintain minimum clear space equal to the icon width on all sides.</p>
          </div>
          <div class="brand-rule-item">
            <span class="brand-rule-icon brand-rule-icon--dont">&#10007;</span>
            <p>Never rotate, skew, or add effects (drop shadows, gradients) to the mark.</p>
          </div>
          <div class="brand-rule-item">
            <span class="brand-rule-icon brand-rule-icon--dont">&#10007;</span>
            <p>Never use "PaperclipCN", "paperclip cn", or the older "Penclip" form on Paperclip CN brand surfaces. Always write it with a space: Paperclip CN.</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Color -->
    <section class="brand-section" id="color">
      <div class="container">
        <hr class="brand-rule">
        <span class="brand-badge">Color</span>
        <h2 class="heading-serif">Warm, grounded neutrals<br>with one active accent.</h2>
        <p class="brand-lede">The palette is intentionally restrained — warm off-whites and near-blacks create a refined editorial feel. Green signals life, activity, and success.</p>

        <div class="brand-palette">
          <div class="brand-swatch-group">
            <h3 class="brand-subsection">Foundations</h3>
            <div class="brand-swatch-row">
              <div class="brand-swatch" style="background:#ffffff; border:1px solid #e0dcd6;">
                <div class="brand-swatch-color"></div>
                <div class="brand-swatch-info">
                  <span class="brand-swatch-name">White</span>
                  <code class="brand-swatch-hex">#ffffff</code>
                  <span class="brand-swatch-role">Primary background</span>
                </div>
              </div>
              <div class="brand-swatch" style="background:#f5f3f0;">
                <div class="brand-swatch-color"></div>
                <div class="brand-swatch-info">
                  <span class="brand-swatch-name">Warm Linen</span>
                  <code class="brand-swatch-hex">#f5f3f0</code>
                  <span class="brand-swatch-role">Alt background, badges</span>
                </div>
              </div>
              <div class="brand-swatch" style="background:#f0ece7;">
                <div class="brand-swatch-color"></div>
                <div class="brand-swatch-info">
                  <span class="brand-swatch-name">Parchment</span>
                  <code class="brand-swatch-hex">#f0ece7</code>
                  <span class="brand-swatch-role">Surface, cards</span>
                </div>
              </div>
              <div class="brand-swatch" style="background:#e0dcd6;">
                <div class="brand-swatch-color"></div>
                <div class="brand-swatch-info">
                  <span class="brand-swatch-name">Stone</span>
                  <code class="brand-swatch-hex">#e0dcd6</code>
                  <span class="brand-swatch-role">Borders, dividers</span>
                </div>
              </div>
            </div>
          </div>

          <div class="brand-swatch-group">
            <h3 class="brand-subsection">Text</h3>
            <div class="brand-swatch-row">
              <div class="brand-swatch" style="background:#1a1a1a;">
                <div class="brand-swatch-color"></div>
                <div class="brand-swatch-info brand-swatch-info--light">
                  <span class="brand-swatch-name">Ink</span>
                  <code class="brand-swatch-hex">#1a1a1a</code>
                  <span class="brand-swatch-role">Primary text, headings</span>
                </div>
              </div>
              <div class="brand-swatch" style="background:#4a4a4a;">
                <div class="brand-swatch-color"></div>
                <div class="brand-swatch-info brand-swatch-info--light">
                  <span class="brand-swatch-name">Graphite</span>
                  <code class="brand-swatch-hex">#4a4a4a</code>
                  <span class="brand-swatch-role">Secondary text</span>
                </div>
              </div>
              <div class="brand-swatch" style="background:#888880;">
                <div class="brand-swatch-color"></div>
                <div class="brand-swatch-info brand-swatch-info--light">
                  <span class="brand-swatch-name">Ash</span>
                  <code class="brand-swatch-hex">#888880</code>
                  <span class="brand-swatch-role">Muted text, captions</span>
                </div>
              </div>
            </div>
          </div>

          <div class="brand-swatch-group">
            <h3 class="brand-subsection">Accent &amp; Status</h3>
            <div class="brand-swatch-row">
              <div class="brand-swatch" style="background:#22c55e;">
                <div class="brand-swatch-color"></div>
                <div class="brand-swatch-info brand-swatch-info--light">
                  <span class="brand-swatch-name">Signal Green</span>
                  <code class="brand-swatch-hex">#22c55e</code>
                  <span class="brand-swatch-role">Active, success, alive</span>
                </div>
              </div>
              <div class="brand-swatch" style="background:#dcfce7;">
                <div class="brand-swatch-color"></div>
                <div class="brand-swatch-info">
                  <span class="brand-swatch-name">Mint Wash</span>
                  <code class="brand-swatch-hex">#dcfce7</code>
                  <span class="brand-swatch-role">Task labels, light green bg</span>
                </div>
              </div>
              <div class="brand-swatch" style="background:#f59e0b;">
                <div class="brand-swatch-color"></div>
                <div class="brand-swatch-info brand-swatch-info--light">
                  <span class="brand-swatch-name">Amber Alert</span>
                  <code class="brand-swatch-hex">#f59e0b</code>
                  <span class="brand-swatch-role">Warnings, interrupts</span>
                </div>
              </div>
              <div class="brand-swatch" style="background:#0c0c0c;">
                <div class="brand-swatch-color"></div>
                <div class="brand-swatch-info brand-swatch-info--light">
                  <span class="brand-swatch-name">Void</span>
                  <code class="brand-swatch-hex">#0c0c0c</code>
                  <span class="brand-swatch-role">Dark sections, terminal</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Typography -->
    <section class="brand-section" id="typography">
      <div class="container">
        <hr class="brand-rule">
        <span class="brand-badge">Typography</span>
        <h2 class="heading-serif">Three typefaces.<br>Each with a clear job.</h2>

        <div class="brand-type-specimens">
          <div class="brand-type-card">
            <span class="brand-type-badge">Headings</span>
            <p class="brand-type-sample brand-type-sample--serif">Instrument Serif</p>
            <p class="brand-type-meta">Regular 400 &middot; Italic</p>
            <p class="brand-type-desc">The editorial voice. Used for all headings and display text. Weight 400 only — the delicacy of the serif strokes provides natural emphasis without needing bold.</p>
            <div class="brand-type-specimen">
              <p style="font-family:'Instrument Serif',serif;font-size:clamp(2rem,4vw,2.8rem);line-height:1.15;letter-spacing:-0.01em;">Paperclip CN runs your business.</p>
            </div>
            <div class="brand-type-tokens">
              <code>font-family: 'Instrument Serif', serif</code>
              <code>line-height: 1.15</code>
              <code>letter-spacing: -0.01em</code>
            </div>
          </div>

          <div class="brand-type-card">
            <span class="brand-type-badge">Body</span>
            <p class="brand-type-sample brand-type-sample--sans">Inter</p>
            <p class="brand-type-meta">400 Regular &middot; 500 Medium &middot; 600 Semibold</p>
            <p class="brand-type-desc">The workhorse. Used for body text, labels, navigation, and UI elements. Highly legible at small sizes. Semibold for the wordmark and emphasis.</p>
            <div class="brand-type-specimen">
              <p style="font-family:'Inter',sans-serif;font-size:1rem;line-height:1.65;color:#4a4a4a;">Open-source orchestration to manage teams of AI Agents. Hire AI employees, set goals, automate jobs and your business runs itself.</p>
            </div>
            <div class="brand-type-tokens">
              <code>font-family: 'Inter', sans-serif</code>
              <code>line-height: 1.6–1.7</code>
              <code>font-size: 0.8rem–1.15rem</code>
            </div>
          </div>

          <div class="brand-type-card">
            <span class="brand-type-badge">Code</span>
            <p class="brand-type-sample brand-type-sample--mono">JetBrains Mono</p>
            <p class="brand-type-meta">400 Regular &middot; 500 Medium</p>
            <p class="brand-type-desc">The technical layer. Used in terminal blocks, code snippets, inline tokens, and the CLI. Gives the interface its engineering credibility.</p>
            <div class="brand-type-specimen brand-type-specimen--dark">
              <p style="font-family:'JetBrains Mono',monospace;font-size:0.92rem;line-height:1.6;color:#f0f0f0;">$ npx penclip onboard --yes<br><span style="color:#22c55e">✓</span> Company created<br><span style="color:#22c55e">✓</span> CEO agent online</p>
            </div>
            <div class="brand-type-tokens">
              <code>font-family: 'JetBrains Mono', monospace</code>
              <code>font-size: 0.92rem–0.95rem</code>
            </div>
          </div>
        </div>

        <div class="brand-type-scale">
          <h3 class="brand-subsection">Type Scale</h3>
          <div class="brand-scale-table">
          <div class="brand-scale-row"><span class="brand-scale-label">Hero</span><span class="brand-scale-sample" style="font-family:'Instrument Serif',serif;font-size:clamp(1.8rem,4vw,3rem);line-height:1.15;">Paperclip CN runs your business.</span><code class="brand-scale-size">clamp(2.8rem, 7vw, 4.5rem)</code></div>
            <div class="brand-scale-row"><span class="brand-scale-label">H2</span><span class="brand-scale-sample" style="font-family:'Instrument Serif',serif;font-size:clamp(1.4rem,3vw,2rem);line-height:1.15;">Section heading</span><code class="brand-scale-size">clamp(2rem, 4vw, 2.8rem)</code></div>
            <div class="brand-scale-row"><span class="brand-scale-label">Body</span><span class="brand-scale-sample" style="font-family:'Inter',sans-serif;font-size:1rem;line-height:1.65;">Body text at standard size</span><code class="brand-scale-size">1rem / 16px</code></div>
            <div class="brand-scale-row"><span class="brand-scale-label">Small</span><span class="brand-scale-sample" style="font-family:'Inter',sans-serif;font-size:0.8rem;line-height:1.5;font-weight:500;">Labels and badges</span><code class="brand-scale-size">0.8rem / 12.8px</code></div>
            <div class="brand-scale-row"><span class="brand-scale-label">Mono</span><span class="brand-scale-sample" style="font-family:'JetBrains Mono',monospace;font-size:0.92rem;line-height:1.5;">code --inline</span><code class="brand-scale-size">0.92rem / 14.7px</code></div>
          </div>
        </div>
      </div>
    </section>

    <!-- Voice -->
    <section class="brand-section brand-section--warm" id="voice">
      <div class="container">
        <hr class="brand-rule">
        <span class="brand-badge">Voice &amp; Tone</span>
        <h2 class="heading-serif">Direct. Confident.<br>Never decorative.</h2>
        <p class="brand-lede">Paperclip CN speaks with authority about complex systems but never obscures meaning. Short sentences. Active verbs. Technical when needed, never when not.</p>

        <div class="brand-voice-grid">
          <div class="brand-voice-card">
            <h3 class="brand-voice-trait">Clarity</h3>
            <p class="brand-voice-desc">Explain complex concepts simply. One idea per sentence. If a word doesn't earn its place, cut it.</p>
            <div class="brand-voice-example"><span class="brand-voice-label">Example</span><blockquote>"Paperclip CN uses those agents. It orchestrates them into a company — with org charts, budgets, goals, governance, and accountability."</blockquote></div>
          </div>
          <div class="brand-voice-card">
            <h3 class="brand-voice-trait">Control</h3>
            <p class="brand-voice-desc">Emphasize that the human is always in charge. Use imperative verbs. Short, punchy sequences.</p>
            <div class="brand-voice-example"><span class="brand-voice-label">Example</span><blockquote>"Pause. Resume. Override. Reassign. Terminate. You have full control over every agent in the org."</blockquote></div>
          </div>
          <div class="brand-voice-card">
            <h3 class="brand-voice-trait">Honesty</h3>
            <p class="brand-voice-desc">Say what Paperclip CN is and what it is not. Never oversell. Let the product speak through specificity, not superlatives.</p>
            <div class="brand-voice-example"><span class="brand-voice-label">Example</span><blockquote>"Not a chatbot. Not an agent framework. Not a workflow builder."</blockquote></div>
          </div>
          <div class="brand-voice-card">
            <h3 class="brand-voice-trait">Credibility</h3>
            <p class="brand-voice-desc">Use precise technical language where it matters. Observable, traceable, logged — these are features, not marketing.</p>
            <div class="brand-voice-example"><span class="brand-voice-label">Example</span><blockquote>"Full observability. Every ticket traced. Every decision explained. Every tool call logged."</blockquote></div>
          </div>
        </div>

        <div class="brand-voice-donts">
          <h3 class="brand-subsection">Avoid</h3>
          <ul class="brand-dont-list">
            <li>Buzzwords without substance ("revolutionary", "game-changing", "next-gen")</li>
            <li>Passive voice where active is clearer</li>
            <li>Vague claims ("powerful", "robust", "scalable") without specifics</li>
            <li>Exclamation marks in product copy</li>
            <li>Emoji in technical documentation</li>
          </ul>
        </div>
      </div>
    </section>

    <!-- Components -->
    <section class="brand-section" id="components">
      <div class="container">
        <hr class="brand-rule">
        <span class="brand-badge">Components</span>
        <h2 class="heading-serif">Living elements.</h2>
        <p class="brand-lede">Core UI patterns used across the Paperclip CN interface. Pill-shaped buttons, warm-bordered cards, glassmorphic overlays, and a macOS-style terminal.</p>

        <div class="brand-comp-section">
          <h3 class="brand-subsection">Buttons</h3>
          <div class="brand-comp-row">
            <div class="brand-comp-demo">
              <button class="brand-btn brand-btn--primary">Get started</button>
              <span class="brand-comp-label">Primary</span>
            </div>
            <div class="brand-comp-demo">
              <button class="brand-btn brand-btn--secondary">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 19c-5 1.5-5-2.5-7-3m14 6v-3.87a3.37 3.37 0 0 0-.94-2.61c3.14-.35 6.44-1.54 6.44-7A5.44 5.44 0 0 0 20 4.77 5.07 5.07 0 0 0 19.91 1S18.73.65 16 2.48a13.38 13.38 0 0 0-7 0C6.27.65 5.09 1 5.09 1A5.07 5.07 0 0 0 5 4.77a5.44 5.44 0 0 0-1.5 3.78c0 5.42 3.3 6.61 6.44 7A3.37 3.37 0 0 0 9 18.13V22"/></svg>
                Star on GitHub
              </button>
              <span class="brand-comp-label">Secondary</span>
            </div>
            <div class="brand-comp-demo brand-comp-demo--dark">
              <button class="brand-btn brand-btn--glass">Read the docs</button>
              <span class="brand-comp-label" style="color:rgba(255,255,255,0.45);">Glass (on dark)</span>
            </div>
          </div>
          <div class="brand-comp-tokens"><code>border-radius: 100px (pill)</code><code>padding: 0.8rem 2rem</code><code>font-weight: 500</code><code>transition: 0.2s ease</code></div>
        </div>

        <div class="brand-comp-section">
          <h3 class="brand-subsection">Cards</h3>
          <div class="brand-comp-row">
            <div class="brand-card-example">
              <div class="brand-card-demo">
                <div class="brand-card-icon"><svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M12 6v6l4 2"/></svg></div>
                <div class="brand-card-text"><strong>Schedule</strong><span>Agents wake on schedule or on demand. Every action logged.</span></div>
              </div>
              <span class="brand-comp-label">Feature card</span>
            </div>
            <div class="brand-card-example">
              <div class="brand-card-demo brand-card-demo--active">
                <div class="brand-card-dot"></div>
                <div class="brand-card-text"><strong>ClaudeCoder</strong><span style="color:#22c55e">Active</span></div>
              </div>
              <span class="brand-comp-label">Agent card (active)</span>
            </div>
          </div>
          <div class="brand-comp-tokens"><code>border: 1.5px solid var(--color-border)</code><code>border-radius: 10px</code><code>padding: 12px 14px</code><code>background: var(--color-bg)</code></div>
        </div>

        <div class="brand-comp-section">
          <h3 class="brand-subsection">Terminal</h3>
          <div class="brand-terminal">
            <div class="brand-terminal-header">
              <span class="brand-terminal-dot" style="background:#ff5f57;"></span>
              <span class="brand-terminal-dot" style="background:#febc2e;"></span>
              <span class="brand-terminal-dot" style="background:#28c840;"></span>
            </div>
            <div class="brand-terminal-body">
              <div class="brand-terminal-line"><span class="brand-terminal-prompt">$</span> penclip init</div>
              <div class="brand-terminal-line brand-terminal-output">Creating company... <span style="color:#22c55e">done</span></div>
              <div class="brand-terminal-line brand-terminal-output">Hiring CEO agent... <span style="color:#22c55e">done</span></div>
              <div class="brand-terminal-line brand-terminal-output">Deploying to localhost:3100... <span style="color:#22c55e">done</span></div>
              <div class="brand-terminal-line"><span class="brand-terminal-prompt">$</span> <span class="brand-terminal-cursor">|</span></div>
            </div>
          </div>
          <div class="brand-comp-tokens"><code>background: #1a1a1a</code><code>font-family: JetBrains Mono</code><code>font-size: 0.92rem</code><code>border-radius: 16px</code></div>
        </div>

        <div class="brand-comp-section">
          <h3 class="brand-subsection">Badges</h3>
          <div class="brand-comp-row" style="gap:1rem;">
            <span class="brand-badge-demo">How it works</span>
            <span class="brand-badge-demo brand-badge-demo--green">Active</span>
            <span class="brand-badge-demo brand-badge-demo--task">PAP-193</span>
          </div>
          <div class="brand-comp-tokens"><code>border-radius: 100px</code><code>padding: 0.4em 1.1em</code><code>font-size: 0.8rem</code><code>font-weight: 500</code></div>
        </div>

        <div class="brand-comp-section">
          <h3 class="brand-subsection">Navbar (Glassmorphism)</h3>
          <div class="brand-navbar-wrapper">
            <div class="brand-navbar-demo">
              <div class="brand-navbar-left"><span style="font-family:'JetBrains Mono',monospace;font-size:0.82rem;opacity:0.7;">Docs</span><span style="font-family:'JetBrains Mono',monospace;font-size:0.82rem;opacity:0.7;">GitHub</span></div>
              <div class="brand-navbar-center">
                <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M21.44 11.05l-9.19 9.19a6 6 0 0 1-8.49-8.49l9.19-9.19a4 4 0 0 1 5.66 5.66l-9.2 9.19a2 2 0 0 1-2.83-2.83l8.49-8.48" /></svg>
                <span style="font-weight:600;font-size:0.92rem;">{BRAND.name}</span>
              </div>
              <div class="brand-navbar-right"><button class="brand-btn--glass" style="padding:0.35rem 0.8rem;font-size:0.78rem;border-radius:100px;">Star</button></div>
            </div>
          </div>
          <div class="brand-comp-tokens"><code>background: rgba(255,255,255,0.12)</code><code>backdrop-filter: blur(20px) saturate(1.4)</code><code>border: 1px solid rgba(255,255,255,0.2)</code></div>
        </div>
      </div>
    </section>

    <!-- Spacing -->
    <section class="brand-section" id="spacing">
      <div class="container">
        <hr class="brand-rule">
        <span class="brand-badge">Spacing &amp; Layout</span>
        <h2 class="heading-serif">Generous whitespace.<br>Fluid scale.</h2>
        <p class="brand-lede">All spacing uses CSS clamp() for fluid responsiveness. Container max-width is 1140px. Breakpoints at 768px, 640px, and 480px.</p>

        <div class="brand-spacing-tokens">
          <div class="brand-spacing-row"><code class="brand-spacing-name">--space-xs</code><div class="brand-spacing-bar" style="width:0.5rem;"></div><span class="brand-spacing-value">0.5rem / 8px</span></div>
          <div class="brand-spacing-row"><code class="brand-spacing-name">--space-sm</code><div class="brand-spacing-bar" style="width:1rem;"></div><span class="brand-spacing-value">1rem / 16px</span></div>
          <div class="brand-spacing-row"><code class="brand-spacing-name">--space-md</code><div class="brand-spacing-bar" style="width:1.5rem;"></div><span class="brand-spacing-value">1.5rem / 24px</span></div>
          <div class="brand-spacing-row"><code class="brand-spacing-name">--space-lg</code><div class="brand-spacing-bar" style="width:3rem;"></div><span class="brand-spacing-value">clamp(2rem, 1.5rem + 1.5vw, 3rem)</span></div>
          <div class="brand-spacing-row"><code class="brand-spacing-name">--space-xl</code><div class="brand-spacing-bar" style="width:5rem;"></div><span class="brand-spacing-value">clamp(3rem, 2rem + 3vw, 5rem)</span></div>
          <div class="brand-spacing-row"><code class="brand-spacing-name">--space-section</code><div class="brand-spacing-bar" style="width:9rem;"></div><span class="brand-spacing-value">clamp(5rem, 3rem + 5.5vw, 9rem)</span></div>
        </div>

        <div class="brand-radius-tokens">
          <h3 class="brand-subsection">Border Radius</h3>
          <div class="brand-radius-row">
            <div class="brand-radius-demo" style="border-radius:10px;"><span>10px</span><code>--radius-sm</code></div>
            <div class="brand-radius-demo" style="border-radius:16px;"><span>16px</span><code>--radius</code></div>
            <div class="brand-radius-demo" style="border-radius:24px;"><span>24px</span><code>--radius-lg</code></div>
            <div class="brand-radius-demo brand-radius-demo--pill" style="border-radius:100px;"><span>pill</span><code>--radius-pill</code></div>
          </div>
        </div>
      </div>
    </section>

    <!-- Photography -->
    <section class="brand-section brand-section--dark" id="photography">
      <div class="container">
        <hr class="brand-rule" style="border-color:rgba(255,255,255,0.1);">
        <span class="brand-badge" style="background:rgba(255,255,255,0.08);color:rgba(255,255,255,0.7);">Photography</span>
        <h2 class="heading-serif" style="color:#f0f0f0;">Atmospheric.<br>Never stock.</h2>
        <p class="brand-lede" style="color:rgba(255,255,255,0.55);">Background photography sets mood, not information. Images are always overlaid with gradients — they recede behind content, never compete with it.</p>

        <div class="brand-photo-grid">
          <div class="brand-photo-card">
            <div class="brand-photo-frame">
              <picture><source srcset="/hero-bg.webp" type="image/webp"><img src="/hero-bg.jpg" alt="Hero background" loading="lazy"></picture>
              <div class="brand-photo-overlay brand-photo-overlay--hero"></div>
            </div>
            <div class="brand-photo-meta"><span class="brand-photo-name">Hero background</span><span class="brand-photo-treatment">Gradient fade: transparent → white</span></div>
          </div>
          <div class="brand-photo-card">
            <div class="brand-photo-frame">
              <picture><source srcset="/footer-bg.webp" type="image/webp"><img src="/footer-bg.jpg" alt="Footer background" loading="lazy"></picture>
              <div class="brand-photo-overlay brand-photo-overlay--footer"></div>
            </div>
            <div class="brand-photo-meta"><span class="brand-photo-name">Footer background</span><span class="brand-photo-treatment">Overlay: dark blue-gray at 55–70% opacity</span></div>
          </div>
        </div>
      </div>
    </section>

    <!-- Motion -->
    <section class="brand-section" id="animation">
      <div class="container">
        <hr class="brand-rule">
        <span class="brand-badge">Motion</span>
        <h2 class="heading-serif">Purposeful, never showy.</h2>
        <p class="brand-lede">Animation signals life and state. Entrance reveals use staggered delays. Active elements pulse. Transitions ease at 0.2s. Nothing bounces unless it has a reason.</p>

        <div class="brand-motion-grid">
          <div class="brand-motion-card">
            <div class="brand-motion-demo brand-motion-demo--fade">
              <div class="brand-motion-box"></div>
              <div class="brand-motion-box" style="animation-delay:140ms;"></div>
              <div class="brand-motion-box" style="animation-delay:280ms;"></div>
            </div>
            <h4 class="brand-motion-name">Staggered entrance</h4>
            <p class="brand-motion-desc">Items appear sequentially with 140ms delay. translateY: 12px → 0, opacity: 0 → 1.</p>
            <code class="brand-motion-spec">0.45s ease-out, stagger: 140ms</code>
          </div>
          <div class="brand-motion-card">
            <div class="brand-motion-demo"><div class="brand-motion-pulse"></div></div>
            <h4 class="brand-motion-name">Heartbeat pulse</h4>
            <p class="brand-motion-desc">Active agent indicator. Scale 1 → 1.18, infinite loop. Green glow expands and fades.</p>
            <code class="brand-motion-spec">2s ease-in-out infinite</code>
          </div>
          <div class="brand-motion-card">
            <div class="brand-motion-demo"><div class="brand-motion-hover">Hover me</div></div>
            <h4 class="brand-motion-name">Hover transition</h4>
            <p class="brand-motion-desc">Simple opacity or color shift on interactive elements. No transform, no bounce.</p>
            <code class="brand-motion-spec">0.2s ease</code>
          </div>
        </div>
      </div>
    </section>

    <!-- Design Tokens -->
    <section class="brand-section brand-section--warm" id="tokens">
      <div class="container">
        <hr class="brand-rule">
        <span class="brand-badge">Quick Reference</span>
        <h2 class="heading-serif">Design tokens.</h2>

        <div class="brand-terminal" style="max-width:640px;">
          <div class="brand-terminal-header">
            <span class="brand-terminal-dot" style="background:#ff5f57;"></span>
            <span class="brand-terminal-dot" style="background:#febc2e;"></span>
            <span class="brand-terminal-dot" style="background:#28c840;"></span>
          </div>
          <div class="brand-terminal-body">
            <div class="brand-terminal-line brand-terminal-comment">/* Paperclip CN Design Tokens */</div>
            <div class="brand-terminal-line"><span class="brand-terminal-key">--color-bg:</span> #ffffff;</div>
            <div class="brand-terminal-line"><span class="brand-terminal-key">--color-bg-alt:</span> #f5f3f0;</div>
            <div class="brand-terminal-line"><span class="brand-terminal-key">--color-surface:</span> #f0ece7;</div>
            <div class="brand-terminal-line"><span class="brand-terminal-key">--color-border:</span> #e0dcd6;</div>
            <div class="brand-terminal-line"><span class="brand-terminal-key">--color-text:</span> #1a1a1a;</div>
            <div class="brand-terminal-line"><span class="brand-terminal-key">--color-text-secondary:</span> #4a4a4a;</div>
            <div class="brand-terminal-line"><span class="brand-terminal-key">--color-text-muted:</span> #888880;</div>
            <div class="brand-terminal-line"><span class="brand-terminal-key">--color-active:</span> #22c55e;</div>
            <div class="brand-terminal-line"><span class="brand-terminal-key">--color-warning:</span> #f59e0b;</div>
            <div class="brand-terminal-line"></div>
            <div class="brand-terminal-line"><span class="brand-terminal-key">--radius:</span> 16px;</div>
            <div class="brand-terminal-line"><span class="brand-terminal-key">--radius-sm:</span> 10px;</div>
            <div class="brand-terminal-line"><span class="brand-terminal-key">--radius-pill:</span> 100px;</div>
            <div class="brand-terminal-line"></div>
            <div class="brand-terminal-line"><span class="brand-terminal-key">--font-serif:</span> 'Instrument Serif', serif;</div>
            <div class="brand-terminal-line"><span class="brand-terminal-key">--font-sans:</span> 'Inter', sans-serif;</div>
            <div class="brand-terminal-line"><span class="brand-terminal-key">--font-mono:</span> 'JetBrains Mono', mono;</div>
          </div>
        </div>
      </div>
    </section>

  </main>

  <Footer />
</Layout>

<script>
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
        observer.unobserve(entry.target);
      }
    });
  }, { threshold: 0.1, rootMargin: '0px 0px -40px 0px' });
  document.querySelectorAll('.brand-section').forEach(el => observer.observe(el));
</script>

<style>
  /* Brand Page Sections */
  .brand-section { padding: var(--space-section) 0; opacity: 0; transform: translateY(16px); transition: opacity 0.6s ease, transform 0.6s ease; }
  .brand-section.visible { opacity: 1; transform: translateY(0); }
  .brand-section--warm { background: var(--color-bg-alt); }
  .brand-section--dark { background: #0c0c0c; color: #f0f0f0; }

  .brand-rule { border: none; border-top: 1px solid var(--color-border); margin-bottom: var(--space-lg); }
  .brand-badge { display: inline-block; font-size: 0.8rem; font-weight: 500; padding: 0.4em 1.1em; background: var(--color-bg-alt); border-radius: 100px; margin-bottom: var(--space-md); color: var(--color-text-secondary); }
  .brand-lede { font-size: 1.05rem; line-height: 1.65; color: var(--color-text-muted); max-width: 640px; margin-bottom: var(--space-xl); }
  .brand-subsection { font-family: var(--font-mono); font-size: 0.82rem; font-weight: 500; letter-spacing: 0.03em; text-transform: uppercase; color: var(--color-text-muted); margin-bottom: var(--space-md); }

  /* Cover */
  .brand-cover { position: relative; height: 100vh; min-height: 540px; display: flex; align-items: center; justify-content: center; overflow: hidden; }
  .brand-cover-bg { position: absolute; inset: 0; z-index: 0; }
  .brand-cover-bg img { width: 100%; height: 100%; object-fit: cover; object-position: center; }
  .brand-cover-fade { position: absolute; inset: 0; z-index: 1; background: linear-gradient(to bottom, rgba(255,255,255,0.15) 0%, rgba(255,255,255,0.5) 35%, rgba(255,255,255,0.88) 65%, #ffffff 100%); }
  .brand-cover-content { position: relative; z-index: 2; text-align: center; animation: brandCoverReveal 1.2s ease-out; }
  .brand-cover-logo { margin-bottom: 1.5rem; color: var(--color-text); }
  .brand-cover-logo svg { filter: drop-shadow(0 2px 8px rgba(0,0,0,0.06)); }
  .brand-cover-title { font-family: var(--font-serif); font-weight: 400; font-size: clamp(3.5rem, 8vw, 6rem); line-height: 1; letter-spacing: -0.02em; color: var(--color-text); margin-bottom: 0.5rem; }
  .brand-cover-subtitle { font-size: 1rem; font-weight: 500; letter-spacing: 0.15em; text-transform: uppercase; color: var(--color-text-secondary); margin-bottom: 2rem; }
  .brand-cover-version { font-family: var(--font-mono); font-size: 0.78rem; color: var(--color-text-muted); }
  @keyframes brandCoverReveal { from { opacity: 0; transform: translateY(24px); } to { opacity: 1; transform: translateY(0); } }

  /* Intro */
  .brand-intro-grid { display: grid; grid-template-columns: 5fr 7fr; gap: var(--space-xl); align-items: start; }
  .brand-intro-text { font-size: 1.05rem; line-height: 1.7; color: var(--color-text-secondary); margin-bottom: 1rem; }
  .brand-muted { color: var(--color-text-muted); }

  /* Logo */
  .brand-logo-showcase { display: grid; grid-template-columns: repeat(4, 1fr); gap: 1px; background: var(--color-border); border-radius: 16px; overflow: hidden; margin-bottom: var(--space-xl); }
  .brand-logo-card { display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 1.5rem; padding: 3rem 2rem; }
  .brand-logo-card--light { background: var(--color-bg); }
  .brand-logo-card--dark { background: #0c0c0c; }
  .brand-logo-lockup { display: flex; align-items: center; gap: 0.6rem; }
  .brand-logo-wordmark { font-weight: 600; font-size: 1.1rem; }
  .brand-logo-label { font-size: 0.75rem; color: var(--color-text-muted); font-weight: 500; }
  .brand-logo-rules { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; }
  .brand-rule-item { display: flex; gap: 0.75rem; align-items: flex-start; padding: 1rem 1.25rem; background: var(--color-bg-alt); border-radius: 10px; }
  .brand-rule-item p { font-size: 0.9rem; line-height: 1.5; color: var(--color-text-secondary); }
  .brand-rule-icon { flex-shrink: 0; width: 22px; height: 22px; display: flex; align-items: center; justify-content: center; border-radius: 50%; font-size: 0.7rem; font-weight: 700; }
  .brand-rule-icon--do { background: #dcfce7; color: #166534; }
  .brand-rule-icon--dont { background: #fef2f2; color: #991b1b; }

  /* Color */
  .brand-palette { display: flex; flex-direction: column; gap: var(--space-lg); }
  .brand-swatch-row { display: grid; grid-template-columns: repeat(auto-fill, minmax(180px, 1fr)); gap: 1px; background: var(--color-border); border-radius: 10px; overflow: hidden; }
  .brand-swatch { padding: 0; display: flex; flex-direction: column; }
  .brand-swatch-color { height: 72px; }
  .brand-swatch-info { padding: 0.75rem 1rem; background: var(--color-bg); }
  .brand-swatch-info--light { background: transparent; color: #f0f0f0; }
  .brand-swatch-name { display: block; font-size: 0.85rem; font-weight: 500; margin-bottom: 2px; }
  .brand-swatch-hex { display: block; font-family: var(--font-mono); font-size: 0.78rem; color: var(--color-text-muted); margin-bottom: 4px; }
  .brand-swatch-info--light .brand-swatch-hex { color: rgba(255,255,255,0.5); }
  .brand-swatch-role { display: block; font-size: 0.72rem; color: var(--color-text-muted); }
  .brand-swatch-info--light .brand-swatch-role { color: rgba(255,255,255,0.4); }

  /* Typography */
  .brand-type-specimens { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1px; background: var(--color-border); border-radius: 16px; overflow: hidden; margin-bottom: var(--space-xl); }
  .brand-type-card { background: var(--color-bg); padding: 2rem; }
  .brand-type-badge { display: inline-block; font-family: var(--font-mono); font-size: 0.7rem; font-weight: 500; letter-spacing: 0.06em; text-transform: uppercase; color: var(--color-text-muted); padding: 0.25em 0.7em; border: 1px solid var(--color-border); border-radius: 4px; margin-bottom: 1.25rem; }
  .brand-type-sample { margin-bottom: 0.5rem; }
  .brand-type-sample--serif { font-family: var(--font-serif); font-size: clamp(1.8rem, 3vw, 2.4rem); line-height: 1.15; }
  .brand-type-sample--sans { font-size: clamp(1.8rem, 3vw, 2.4rem); font-weight: 400; line-height: 1.15; }
  .brand-type-sample--mono { font-family: var(--font-mono); font-size: clamp(1.4rem, 2.5vw, 1.8rem); line-height: 1.15; }
  .brand-type-meta { font-size: 0.82rem; color: var(--color-text-muted); margin-bottom: 1rem; }
  .brand-type-desc { font-size: 0.88rem; line-height: 1.6; color: var(--color-text-secondary); margin-bottom: 1.25rem; }
  .brand-type-specimen { padding: 1.5rem; background: var(--color-bg-alt); border-radius: 10px; margin-bottom: 1rem; }
  .brand-type-specimen--dark { background: #0c0c0c; color: #f0f0f0; }
  .brand-type-tokens { display: flex; flex-direction: column; gap: 4px; }
  .brand-type-tokens code { font-size: 0.72rem; color: var(--color-text-muted); }
  .brand-type-scale { padding-top: var(--space-lg); }
  .brand-scale-table { display: flex; flex-direction: column; }
  .brand-scale-row { display: grid; grid-template-columns: 80px 1fr auto; gap: 1.5rem; align-items: baseline; padding: 1rem 0; border-bottom: 1px solid var(--color-border); }
  .brand-scale-label { font-family: var(--font-mono); font-size: 0.75rem; font-weight: 500; color: var(--color-text-muted); text-transform: uppercase; }
  .brand-scale-size { font-size: 0.72rem; color: var(--color-text-muted); white-space: nowrap; }

  /* Voice */
  .brand-voice-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 1px; background: var(--color-border); border-radius: 16px; overflow: hidden; margin-bottom: var(--space-xl); }
  .brand-voice-card { background: var(--color-bg); padding: 2rem; }
  .brand-voice-trait { font-family: var(--font-serif); font-size: 1.4rem; font-weight: 400; margin-bottom: 0.75rem; }
  .brand-voice-desc { font-size: 0.9rem; line-height: 1.6; color: var(--color-text-secondary); margin-bottom: 1.25rem; }
  .brand-voice-example { padding: 1.25rem; background: var(--color-bg-alt); border-radius: 10px; border-left: 3px solid var(--color-border); }
  .brand-voice-label { display: block; font-family: var(--font-mono); font-size: 0.68rem; font-weight: 500; text-transform: uppercase; letter-spacing: 0.06em; color: var(--color-text-muted); margin-bottom: 0.5rem; }
  .brand-voice-example blockquote { font-family: var(--font-serif); font-style: italic; font-size: 0.95rem; line-height: 1.6; color: var(--color-text-secondary); }
  .brand-dont-list { list-style: none; display: flex; flex-direction: column; gap: 0.5rem; }
  .brand-dont-list li { font-size: 0.9rem; color: var(--color-text-secondary); padding-left: 1.5rem; position: relative; }
  .brand-dont-list li::before { content: '\00d7'; position: absolute; left: 0; color: #991b1b; font-weight: 700; }

  /* Components */
  .brand-comp-section { margin-bottom: var(--space-xl); }
  .brand-comp-row { display: flex; flex-wrap: wrap; gap: 2rem; align-items: flex-end; margin-bottom: var(--space-md); }
  .brand-comp-demo { display: flex; flex-direction: column; align-items: flex-start; gap: 0.75rem; }
  .brand-comp-demo--dark { background: #0c0c0c; padding: 1.5rem 2rem; border-radius: 10px; }
  .brand-comp-label { font-size: 0.72rem; font-family: var(--font-mono); color: var(--color-text-muted); }
  .brand-comp-tokens { display: flex; flex-wrap: wrap; gap: 0.5rem 1.5rem; padding-top: var(--space-sm); border-top: 1px solid var(--color-border); }
  .brand-comp-tokens code { font-size: 0.72rem; color: var(--color-text-muted); }
  .brand-btn { display: inline-flex; align-items: center; gap: 0.5rem; padding: 0.8rem 2rem; font-size: 0.95rem; font-weight: 500; border: none; border-radius: 100px; cursor: pointer; transition: all 0.2s ease; text-decoration: none; }
  .brand-btn--primary { background: var(--color-text); color: var(--color-bg); }
  .brand-btn--primary:hover { opacity: 0.85; }
  .brand-btn--secondary { background: transparent; color: var(--color-text-secondary); border: 1px solid var(--color-border); }
  .brand-btn--secondary:hover { color: var(--color-text); border-color: var(--color-text-secondary); }
  .brand-btn--glass { background: rgba(255,255,255,0.12); color: rgba(255,255,255,0.9); border: 1px solid rgba(255,255,255,0.15); padding: 0.6rem 1.5rem; font-size: 0.88rem; font-weight: 500; border-radius: 100px; cursor: pointer; transition: all 0.2s ease; }
  .brand-btn--glass:hover { background: rgba(255,255,255,0.2); }

  /* Card demos */
  .brand-card-example { display: flex; flex-direction: column; gap: 0.75rem; }
  .brand-card-demo { display: flex; align-items: center; gap: 0.75rem; padding: 12px 14px; border: 1.5px solid var(--color-border); border-radius: 10px; background: var(--color-bg); min-width: 240px; }
  .brand-card-demo--active { border-color: #22c55e; box-shadow: 0 0 0 1px #22c55e, 0 0 12px rgba(34,197,94,0.1); }
  .brand-card-icon { flex-shrink: 0; width: 36px; height: 36px; display: flex; align-items: center; justify-content: center; background: var(--color-bg-alt); border-radius: 10px; color: var(--color-text-secondary); }
  .brand-card-dot { flex-shrink: 0; width: 10px; height: 10px; border-radius: 50%; background: #22c55e; box-shadow: 0 0 6px rgba(34,197,94,0.5); animation: brandDotPulse 2s ease-in-out infinite; }
  @keyframes brandDotPulse { 0%, 100% { transform: scale(1); opacity: 1; } 50% { transform: scale(1.3); opacity: 0.7; } }
  .brand-card-text { display: flex; flex-direction: column; gap: 2px; }
  .brand-card-text strong { font-size: 0.88rem; font-weight: 600; }
  .brand-card-text span { font-size: 0.78rem; color: var(--color-text-muted); line-height: 1.4; }

  /* Terminal */
  .brand-terminal { background: #0c0c0c; border-radius: 16px; overflow: hidden; margin-bottom: var(--space-md); }
  .brand-terminal-header { display: flex; gap: 6px; padding: 12px 16px; background: #222; }
  .brand-terminal-dot { width: 10px; height: 10px; border-radius: 50%; }
  .brand-terminal-body { padding: 1.25rem 1.5rem; }
  .brand-terminal-line { font-family: var(--font-mono); font-size: 0.88rem; line-height: 1.8; color: #f0f0f0; }
  .brand-terminal-output { color: rgba(240,240,240,0.6); }
  .brand-terminal-comment { color: rgba(240,240,240,0.35); font-style: italic; }
  .brand-terminal-key { color: #22c55e; }
  .brand-terminal-prompt { color: rgba(240,240,240,0.4); margin-right: 0.5rem; }
  .brand-terminal-cursor { display: inline-block; animation: brandCursorBlink 1s step-end infinite; }
  @keyframes brandCursorBlink { 0%, 50% { opacity: 1; } 51%, 100% { opacity: 0; } }

  /* Badges */
  .brand-badge-demo { display: inline-block; font-size: 0.8rem; font-weight: 500; padding: 0.4em 1.1em; background: var(--color-bg-alt); border-radius: 100px; color: var(--color-text-secondary); }
  .brand-badge-demo--green { background: #dcfce7; color: #166534; }
  .brand-badge-demo--task { font-family: var(--font-mono); font-size: 0.75rem; background: var(--color-surface); color: var(--color-text-muted); }

  /* Navbar demo */
  .brand-navbar-wrapper { background: url('/hero-bg.jpg') center/cover; padding: 2rem; border-radius: 16px; display: flex; justify-content: center; margin-bottom: var(--space-md); }
  .brand-navbar-demo { display: flex; align-items: center; justify-content: space-between; width: 100%; max-width: 600px; padding: 0.65rem 1.25rem; background: rgba(255,255,255,0.12); backdrop-filter: blur(20px) saturate(1.4); -webkit-backdrop-filter: blur(20px) saturate(1.4); border: 1px solid rgba(255,255,255,0.2); border-radius: 16px; color: white; }
  .brand-navbar-left, .brand-navbar-right { display: flex; align-items: center; gap: 1rem; }
  .brand-navbar-center { display: flex; align-items: center; gap: 0.4rem; }

  /* Spacing */
  .brand-spacing-tokens { display: flex; flex-direction: column; margin-bottom: var(--space-xl); }
  .brand-spacing-row { display: grid; grid-template-columns: 160px 1fr auto; gap: 1.5rem; align-items: center; padding: 0.75rem 0; border-bottom: 1px solid var(--color-border); }
  .brand-spacing-name { font-size: 0.78rem; color: var(--color-text-muted); }
  .brand-spacing-bar { height: 8px; background: #22c55e; border-radius: 4px; opacity: 0.6; }
  .brand-spacing-value { font-family: var(--font-mono); font-size: 0.72rem; color: var(--color-text-muted); white-space: nowrap; }
  .brand-radius-tokens { padding-top: var(--space-lg); }
  .brand-radius-row { display: flex; flex-wrap: wrap; gap: 1.5rem; }
  .brand-radius-demo { width: 100px; height: 100px; border: 1.5px solid var(--color-border); display: flex; flex-direction: column; align-items: center; justify-content: center; gap: 4px; }
  .brand-radius-demo span { font-size: 0.85rem; font-weight: 500; }
  .brand-radius-demo code { font-size: 0.68rem; color: var(--color-text-muted); }
  .brand-radius-demo--pill { width: 140px; height: 56px; }

  /* Photography */
  .brand-photo-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem; }
  .brand-photo-card { display: flex; flex-direction: column; gap: 1rem; }
  .brand-photo-frame { position: relative; border-radius: 16px; overflow: hidden; aspect-ratio: 16 / 9; }
  .brand-photo-frame img { width: 100%; height: 100%; object-fit: cover; }
  .brand-photo-overlay { position: absolute; inset: 0; }
  .brand-photo-overlay--hero { background: linear-gradient(to bottom, transparent 0%, rgba(255,255,255,0.4) 40%, rgba(255,255,255,0.85) 70%, #ffffff 100%); }
  .brand-photo-overlay--footer { background: linear-gradient(to bottom, rgba(20,30,50,0.55) 0%, rgba(20,30,50,0.7) 100%); }
  .brand-photo-meta { display: flex; flex-direction: column; gap: 2px; }
  .brand-photo-name { font-size: 0.88rem; font-weight: 500; color: rgba(255,255,255,0.8); }
  .brand-photo-treatment { font-size: 0.78rem; font-family: var(--font-mono); color: rgba(255,255,255,0.4); }

  /* Motion */
  .brand-motion-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 1px; background: var(--color-border); border-radius: 16px; overflow: hidden; }
  .brand-motion-card { background: var(--color-bg); padding: 2rem; }
  .brand-motion-demo { height: 80px; display: flex; align-items: center; gap: 0.75rem; margin-bottom: 1.25rem; }
  .brand-motion-demo--fade .brand-motion-box { width: 40px; height: 40px; background: #e8e4de; border-radius: 10px; animation: brandStaggerFade 2.5s ease-out infinite; }
  @keyframes brandStaggerFade { 0%, 100% { opacity: 0; transform: translateY(12px); } 20%, 80% { opacity: 1; transform: translateY(0); } }
  .brand-motion-pulse { width: 16px; height: 16px; background: #22c55e; border-radius: 50%; animation: brandHeartPulse 2s ease-in-out infinite; box-shadow: 0 0 0 0 rgba(34,197,94,0.4); }
  @keyframes brandHeartPulse { 0%, 100% { transform: scale(1); box-shadow: 0 0 0 0 rgba(34,197,94,0.4); } 50% { transform: scale(1.18); box-shadow: 0 0 0 8px rgba(34,197,94,0); } }
  .brand-motion-hover { padding: 0.6rem 1.5rem; border: 1.5px solid var(--color-border); border-radius: 10px; font-size: 0.85rem; font-weight: 500; color: var(--color-text-secondary); cursor: pointer; transition: all 0.2s ease; }
  .brand-motion-hover:hover { border-color: var(--color-text); color: var(--color-text); }
  .brand-motion-name { font-size: 0.92rem; font-weight: 600; margin-bottom: 0.5rem; }
  .brand-motion-desc { font-size: 0.82rem; line-height: 1.5; color: var(--color-text-muted); margin-bottom: 0.75rem; }
  .brand-motion-spec { font-size: 0.72rem; color: var(--color-text-muted); }

  /* Responsive */
  @media (max-width: 768px) {
    .brand-intro-grid { grid-template-columns: 1fr; gap: var(--space-lg); }
    .brand-logo-showcase { grid-template-columns: 1fr 1fr; }
    .brand-logo-rules { grid-template-columns: 1fr; }
    .brand-type-specimens { grid-template-columns: 1fr; }
    .brand-voice-grid { grid-template-columns: 1fr; }
    .brand-motion-grid { grid-template-columns: 1fr; }
    .brand-photo-grid { grid-template-columns: 1fr; }
    .brand-swatch-row { grid-template-columns: repeat(auto-fill, minmax(140px, 1fr)); }
    .brand-spacing-row { grid-template-columns: 120px 1fr; }
    .brand-spacing-value { display: none; }
    .brand-scale-row { grid-template-columns: 60px 1fr; }
    .brand-scale-size { display: none; }
    .brand-cover-title { font-size: clamp(2.5rem, 10vw, 4rem); }
  }
  @media (max-width: 480px) {
    .brand-logo-showcase { grid-template-columns: 1fr; }
    .brand-comp-row { flex-direction: column; }
    .brand-radius-row { gap: 1rem; }
  }
</style>
