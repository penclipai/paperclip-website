---
import { getSiteForPath } from '../config/site';

const site = getSiteForPath(Astro.url.pathname);

const content = site.isChinese
  ? {
      badge: '预算与成本',
      heading: ['看清每个智能体的花费。', '也管住每个智能体的预算。'],
      sub:
        '每个智能体都可以设置月度预算。接近上限时容易发现，达到上限后也可以自动暂停。',
      headers: {
        agent: '角色',
        budget: '预算使用',
        cost: '成本'
      },
      total: '总计',
      detail:
        '你可以按角色、任务和项目查看花费分布，方便找出高消耗环节并及时调整预算。'
    }
  : {
      badge: 'Cost Tracking',
      heading: ['Know what every agent costs.', 'Control what every agent spends.'],
      sub:
        'Every agent gets a monthly budget. When they hit it, they stop. Automatically. No runaway costs. No surprise bills. Hard limits, enforced by the system.',
      headers: {
        agent: 'Agent',
        budget: 'Budget Used',
        cost: 'Cost'
      },
      total: 'Total',
      detail:
        'Track costs per agent, per task, per project, per goal. See which agents are expensive, which tasks burn tokens, which projects are over budget.'
    };
---

<section class="feature-section section">
  <div class="container">
    <hr class="section-divider" />
    <div class="feature-header">
      <div>
        <span class="section-badge">{content.badge}</span>
        <h2 class="heading-serif feature-heading">{content.heading[0]}<br />{content.heading[1]}</h2>
        <p class="feature-sub">
          {content.sub}
        </p>
      </div>
    </div>
    <div class="feature-layout">
      <div class="cost-table-wrap">
        <div class="cost-table" id="cost-table">
          <div class="cost-header-row">
            <span class="cost-col-agent">{content.headers.agent}</span>
            <span class="cost-col-bar">{content.headers.budget}</span>
            <span class="cost-col-cost">{content.headers.cost}</span>
          </div>
          <div class="cost-divider"></div>

          <div class="cost-row" data-cost="42" data-budget="60">
            <div class="cost-col-agent">
              <div class="cost-agent-icon">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M2 20h20"/><path d="M5 20V9l7-5 7 5v11"/><path d="M9 20v-5h6v5"/></svg>
              </div>
              <div class="cost-agent-info">
                <span class="cost-agent-name">CEO</span>
                <span class="cost-agent-model">Claude</span>
              </div>
            </div>
            <div class="cost-col-bar">
              <div class="cost-bar-bg"><div class="cost-bar-fill" style="--target-pct: 70%"></div></div>
            </div>
            <div class="cost-col-cost">
              <span class="cost-amount">$<span class="cost-num">0</span></span>
              <span class="cost-budget">/ $60</span>
            </div>
          </div>

          <div class="cost-row" data-cost="12" data-budget="40">
            <div class="cost-col-agent">
              <div class="cost-agent-icon">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="2" y1="12" x2="22" y2="12"/><path d="M12 2a15.3 15.3 0 014 10 15.3 15.3 0 01-4 10 15.3 15.3 0 01-4-10 15.3 15.3 0 014-10z"/></svg>
              </div>
              <div class="cost-agent-info">
                <span class="cost-agent-name">CMO</span>
                <span class="cost-agent-model">OpenClaw</span>
              </div>
            </div>
            <div class="cost-col-bar">
              <div class="cost-bar-bg"><div class="cost-bar-fill" style="--target-pct: 30%"></div></div>
            </div>
            <div class="cost-col-cost">
              <span class="cost-amount">$<span class="cost-num">0</span></span>
              <span class="cost-budget">/ $40</span>
            </div>
          </div>

          <div class="cost-row" data-cost="25" data-budget="50">
            <div class="cost-col-agent">
              <div class="cost-agent-icon">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 00.33 1.82l.06.06a2 2 0 01-2.83 2.83l-.06-.06a1.65 1.65 0 00-1.82-.33 1.65 1.65 0 00-1 1.51V21a2 2 0 01-4 0v-.09A1.65 1.65 0 009 19.4a1.65 1.65 0 00-1.82.33l-.06.06a2 2 0 01-2.83-2.83l.06-.06A1.65 1.65 0 004.68 15a1.65 1.65 0 00-1.51-1H3a2 2 0 010-4h.09A1.65 1.65 0 004.6 9a1.65 1.65 0 00-.33-1.82l-.06-.06a2 2 0 012.83-2.83l.06.06A1.65 1.65 0 009 4.68a1.65 1.65 0 001-1.51V3a2 2 0 014 0v.09a1.65 1.65 0 001 1.51 1.65 1.65 0 001.82-.33l.06-.06a2 2 0 012.83 2.83l-.06.06A1.65 1.65 0 0019.4 9a1.65 1.65 0 001.51 1H21a2 2 0 010 4h-.09a1.65 1.65 0 00-1.51 1z"/></svg>
              </div>
              <div class="cost-agent-info">
                <span class="cost-agent-name">CTO</span>
                <span class="cost-agent-model">Cursor</span>
              </div>
            </div>
            <div class="cost-col-bar">
              <div class="cost-bar-bg"><div class="cost-bar-fill" style="--target-pct: 50%"></div></div>
            </div>
            <div class="cost-col-cost">
              <span class="cost-amount">$<span class="cost-num">0</span></span>
              <span class="cost-budget">/ $50</span>
            </div>
          </div>

          <div class="cost-row" data-cost="26" data-budget="30">
            <div class="cost-col-agent">
              <div class="cost-agent-icon">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M16 4h2a2 2 0 012 2v14a2 2 0 01-2 2H6a2 2 0 01-2-2V6a2 2 0 012-2h2"/><rect x="8" y="2" width="8" height="4" rx="1" ry="1"/></svg>
              </div>
              <div class="cost-agent-info">
                <span class="cost-agent-name">COO</span>
                <span class="cost-agent-model">Claude</span>
              </div>
            </div>
            <div class="cost-col-bar">
              <div class="cost-bar-bg"><div class="cost-bar-fill" style="--target-pct: 87%"></div></div>
            </div>
            <div class="cost-col-cost">
              <span class="cost-amount">$<span class="cost-num">0</span></span>
              <span class="cost-budget">/ $30</span>
            </div>
          </div>

          <div class="cost-row" data-cost="9" data-budget="30">
            <div class="cost-col-agent">
              <div class="cost-agent-icon">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="16 18 22 12 16 6"/><polyline points="8 6 2 12 8 18"/></svg>
              </div>
              <div class="cost-agent-info">
                <span class="cost-agent-name">CodexCoder</span>
                <span class="cost-agent-model">Codex</span>
              </div>
            </div>
            <div class="cost-col-bar">
              <div class="cost-bar-bg"><div class="cost-bar-fill" style="--target-pct: 30%"></div></div>
            </div>
            <div class="cost-col-cost">
              <span class="cost-amount">$<span class="cost-num">0</span></span>
              <span class="cost-budget">/ $30</span>
            </div>
          </div>

          <div class="cost-row" data-cost="26" data-budget="30">
            <div class="cost-col-agent">
              <div class="cost-agent-icon">
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="16 18 22 12 16 6"/><polyline points="8 6 2 12 8 18"/></svg>
              </div>
              <div class="cost-agent-info">
                <span class="cost-agent-name">ClaudeCoder</span>
                <span class="cost-agent-model">Claude</span>
              </div>
            </div>
            <div class="cost-col-bar">
              <div class="cost-bar-bg"><div class="cost-bar-fill" style="--target-pct: 87%"></div></div>
            </div>
            <div class="cost-col-cost">
              <span class="cost-amount">$<span class="cost-num">0</span></span>
              <span class="cost-budget">/ $30</span>
            </div>
          </div>

          <div class="cost-divider"></div>
          <div class="cost-row cost-row--total" data-cost="140" data-budget="240">
            <div class="cost-col-agent">
              <span class="cost-agent-name">{content.total}</span>
            </div>
            <div class="cost-col-bar"></div>
            <div class="cost-col-cost">
              <span class="cost-amount cost-amount--bold">$<span class="cost-num">0</span></span>
              <span class="cost-budget">/ $240</span>
            </div>
          </div>
        </div>
      </div>
      <div class="feature-details">
        <p class="feature-detail-text">
          {content.detail}
        </p>
      </div>
    </div>
  </div>
</section>

<style>
  .feature-header {
    padding-top: var(--space-2xl);
    margin-bottom: var(--space-xl);
  }

  .feature-heading {
    font-size: clamp(2rem, 4vw, 2.8rem);
    margin-bottom: var(--space-sm);
  }

  .feature-sub {
    color: var(--color-text-muted);
    font-size: 1rem;
    line-height: 1.7;
    max-width: 560px;
  }

  .feature-layout {
    display: grid;
    grid-template-columns: 1.3fr 0.7fr;
    gap: var(--space-lg);
    align-items: start;
  }

  .cost-table-wrap {
    background: var(--color-surface);
    border-radius: var(--radius-lg);
    padding: var(--space-lg);
  }

  .cost-table {
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .cost-header-row {
    display: grid;
    grid-template-columns: 160px 1fr 100px;
    gap: var(--space-sm);
    align-items: center;
    padding-bottom: 0.75rem;
  }

  .cost-header-row span {
    font-size: 0.72rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--color-text-muted);
  }

  .cost-divider {
    height: 1px;
    background: var(--color-border);
  }

  .cost-row {
    display: grid;
    grid-template-columns: 160px 1fr 100px;
    gap: var(--space-sm);
    align-items: center;
    padding: 0.65rem 0;
  }

  .cost-col-agent {
    display: flex;
    align-items: center;
    gap: 0.6rem;
  }

  .cost-agent-icon {
    flex-shrink: 0;
    width: 28px;
    height: 28px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: var(--color-bg);
    border-radius: 6px;
    color: var(--color-text-muted);
  }

  .cost-agent-icon svg {
    width: 16px;
    height: 16px;
  }

  .cost-agent-info {
    display: flex;
    flex-direction: column;
    gap: 1px;
    min-width: 0;
  }

  .cost-agent-name {
    font-size: 0.85rem;
    font-weight: 600;
    color: var(--color-text);
    white-space: nowrap;
  }

  .cost-agent-model {
    font-size: 0.68rem;
    color: var(--color-text-muted);
  }

  .cost-col-bar {
    min-width: 0;
  }

  .cost-bar-bg {
    width: 100%;
    height: 18px;
    background: var(--color-surface-raised);
    border-radius: 4px;
    overflow: hidden;
  }

  .cost-bar-fill {
    height: 100%;
    width: 0%;
    background: var(--color-text);
    border-radius: 4px;
    transition: width 1.2s cubic-bezier(0.22, 1, 0.36, 1);
  }

  .cost-bar-fill.animate {
    width: var(--target-pct);
  }

  .cost-col-cost {
    display: flex;
    align-items: baseline;
    gap: 0.3rem;
    font-variant-numeric: tabular-nums;
  }

  .cost-amount {
    font-size: 0.9rem;
    color: var(--color-text-secondary);
  }

  .cost-amount--bold {
    font-weight: 600;
    color: var(--color-text);
  }

  .cost-budget {
    font-size: 0.75rem;
    color: var(--color-text-muted);
  }

  .cost-row--total {
    padding-top: 0.75rem;
  }

  .feature-details {
    padding-top: var(--space-md);
  }

  .feature-detail-text {
    color: var(--color-text-muted);
    font-size: 1rem;
    line-height: 1.7;
    margin-bottom: var(--space-sm);
  }

  @media (max-width: 768px) {
    .feature-layout {
      grid-template-columns: 1fr;
    }

    .cost-header-row,
    .cost-row {
      grid-template-columns: 130px 1fr 90px;
    }
  }

  @media (max-width: 480px) {
    .cost-header-row,
    .cost-row {
      grid-template-columns: 110px 1fr 80px;
    }

    .cost-agent-icon {
      display: none;
    }
  }
</style>

<script>
  const table = document.getElementById('cost-table');
  if (table) {
    const rows = table.querySelectorAll('.cost-row');
    const fills = table.querySelectorAll('.cost-bar-fill');

    function countUp(el, target, duration) {
      const start = performance.now();
      function tick(now) {
        const progress = Math.min((now - start) / duration, 1);
        const eased = 1 - Math.pow(1 - progress, 3);
        el.textContent = Math.round(eased * target);
        if (progress < 1) requestAnimationFrame(tick);
      }
      requestAnimationFrame(tick);
    }

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            // Animate bars
            fills.forEach((fill) => {
              fill.classList.add('animate');
            });

            // Count up cost numbers with stagger
            rows.forEach((row, i) => {
              const cost = parseInt(row.dataset.cost, 10);
              const numEl = row.querySelector('.cost-num');
              if (numEl && cost) {
                setTimeout(() => countUp(numEl, cost, 1000), i * 80);
              }
            });
          } else {
            // Reset when scrolled away
            fills.forEach((fill) => {
              fill.classList.remove('animate');
            });
            rows.forEach((row) => {
              const numEl = row.querySelector('.cost-num');
              if (numEl) numEl.textContent = '0';
            });
          }
        });
      },
      { threshold: 0.3 }
    );

    observer.observe(table);
  }
</script>
