---
import { CURRENT_SITE } from '../config/site';

const { hero } = CURRENT_SITE.home;
---

<section class="hero">
  <div class="hero-image">
    <picture>
      <source srcset="/hero-bg.webp" type="image/webp" />
      <img src="/hero-bg.jpg" alt="" width="1920" height="759" loading="eager" />
    </picture>
  </div>
  <div class="container hero-inner">
    <p class="hero-eyebrow">{hero.eyebrow}</p>
    <h1 class="hero-headline heading-serif">
      {hero.headlineLines.map((line) => <span class="hero-line">{line}</span>)}
    </h1>
    <p class="hero-lede">
      {hero.lede}
    </p>
    <div class="hero-actions">
      <a href={hero.primaryCtaHref} class="hero-btn-primary">{hero.primaryCtaLabel}</a>
      <a href={hero.secondaryCtaHref} class="hero-btn-secondary">
        {hero.secondaryCtaLabel}
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M7 17L17 7"/><path d="M7 7h10v10"/></svg>
      </a>
    </div>
  </div>
</section>

<style>
  .hero {
    position: relative;
  }

  .hero-image {
    position: relative;
    width: 100%;
    height: 65vh;
    min-height: 340px;
    overflow: hidden;
  }

  .hero-image::after {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    right: 0;
    height: 50%;
    background: linear-gradient(to bottom, transparent 0%, rgba(255,255,255,0.4) 40%, rgba(255,255,255,0.85) 70%, #ffffff 100%);
    pointer-events: none;
  }

  .hero-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center center;
    display: block;
  }

  .hero-inner {
    position: relative;
    z-index: 1;
    max-width: 780px;
    text-align: center;
    margin-top: -6rem;
    padding: 0 var(--space-md) var(--space-xl);
  }

  .hero-headline {
    font-size: clamp(2.8rem, 7vw, 4.5rem);
    margin-bottom: 0.75rem;
  }

  .hero-eyebrow {
    font-size: 0.82rem;
    font-weight: 500;
    color: var(--color-text-secondary);
    margin: 0 0 0.65rem;
    letter-spacing: 0.04em;
  }

  .hero-line {
    display: block;
  }

  .hero-sub {
    font-size: clamp(1.05rem, 2.2vw, 1.25rem);
    color: var(--color-text-secondary);
    margin-bottom: 0.5rem;
  }

  .hero-lede {
    font-size: 1rem;
    color: var(--color-text-muted);
    max-width: 520px;
    margin: 0 auto 1.5rem;
    line-height: 1.6;
  }

  .hero-actions {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-sm);
    flex-wrap: wrap;
  }

  .hero-btn-primary {
    display: inline-block;
    font-size: 0.95rem;
    font-weight: 500;
    padding: 0.8rem 2rem;
    background: var(--color-text);
    color: var(--color-bg);
    border-radius: var(--radius-pill);
    transition: opacity 0.2s;
  }

  .hero-btn-primary:hover {
    opacity: 0.85;
  }

  .hero-btn-secondary {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    font-size: 0.95rem;
    padding: 0.8rem 2rem;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-pill);
    color: var(--color-text-secondary);
    transition: color 0.2s, border-color 0.2s;
  }

  .hero-btn-secondary:hover {
    color: var(--color-text);
    border-color: var(--color-text-muted);
  }

  @media (max-width: 768px) {
    .hero-image {
      height: 45vh;
      min-height: 260px;
    }

    .hero-inner {
      margin-top: -4rem;
      padding-bottom: var(--space-lg);
    }

    .hero-headline {
      font-size: clamp(2.2rem, 9vw, 3rem);
      margin-bottom: 0.5rem;
    }

    .hero-eyebrow {
      font-size: 0.76rem;
      margin-bottom: 0.45rem;
    }

    .hero-sub {
      margin-bottom: 0.4rem;
    }

    .hero-lede {
      margin-bottom: 1.25rem;
    }
  }

  @media (max-width: 480px) {
    .hero-image {
      height: 38vh;
      min-height: 220px;
    }

    .hero-inner {
      margin-top: -3rem;
    }
  }
</style>
