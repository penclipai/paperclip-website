server {
    listen 80;
    listen [::]:80;
    server_name paperclipai.cn;

    root /var/www/penclip-website/current-cn;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
